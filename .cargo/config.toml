# This crate uses `SyncCell` (mutable statics without synchronisation),
# which is sound in single-threaded WASM guests but requires tests to
# run sequentially to avoid data races in the test harness.

[alias]
nt = "nextest run --lib --test-threads=1 --all-features"
